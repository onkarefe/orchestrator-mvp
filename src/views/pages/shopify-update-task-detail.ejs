<%
  const display = (value) =>
    value === null || value === undefined || value === '' ? '-' : value;
  const statusClass = (value) =>
    `status-${String(value || 'unknown').toLowerCase().replace(/[^a-z0-9]+/g, '-')}`;
  const dryRunLabel = (value) => value ? 'Dry-run' : 'Live candidate';
  const writeBlockReason =
    shopifyUpdateTask.result_json?.externalWriteBlockedReason ||
    shopifyUpdateTask.result_json?.writeSuppressedReason ||
    shopifyUpdateTask.payload_json?.writeSuppressedReason ||
    '-';
%>
<section class="page">
  <header class="page-header page-header-with-actions">
    <div>
      <a class="back-link" href="/admin/shopify-update-tasks">Back to Shopify update tasks</a>
      <p class="eyebrow">Shopify update task detail</p>
      <h1>Shopify Update Task #<%= shopifyUpdateTask.id %></h1>
      <p class="page-description"><%= display(shopifyUpdateTask.task_type) %></p>
    </div>
    <span class="status-badge status-badge-large <%= statusClass(shopifyUpdateTask.status) %>">
      <%= display(shopifyUpdateTask.status) %>
    </span>
  </header>

  <section class="card">
    <div class="card-header">
      <div>
        <p class="section-label">Executor task</p>
        <h2>Update summary</h2>
      </div>
    </div>
    <dl class="meta-grid meta-grid-three">
      <div class="meta-item">
        <dt>Order ID</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.order_id) %></dd>
      </div>
      <div class="meta-item">
        <dt>Shopify order ID</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.shopify_order_id) %></dd>
      </div>
      <div class="meta-item">
        <dt>Task type</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.task_type) %></dd>
      </div>
      <div class="meta-item">
        <dt>Idempotency key</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.idempotency_key) %></dd>
      </div>
      <div class="meta-item">
        <dt>Source</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.source_type) %> / <%= display(shopifyUpdateTask.source_id) %></dd>
      </div>
      <div class="meta-item">
        <dt>Status</dt>
        <dd><span class="status-badge <%= statusClass(shopifyUpdateTask.status) %>"><%= display(shopifyUpdateTask.status) %></span></dd>
      </div>
      <div class="meta-item">
        <dt>Dry-run</dt>
        <dd><span class="status-badge status-neutral"><%= dryRunLabel(shopifyUpdateTask.dry_run) %></span></dd>
      </div>
      <div class="meta-item">
        <dt>Attempts</dt>
        <dd><%= display(shopifyUpdateTask.attempt_count) %> / <%= display(shopifyUpdateTask.max_attempts) %></dd>
      </div>
      <div class="meta-item">
        <dt>Last error</dt>
        <dd><%= display(shopifyUpdateTask.last_error) %></dd>
      </div>
      <div class="meta-item">
        <dt>External write block</dt>
        <dd class="mono"><%= display(writeBlockReason) %></dd>
      </div>
      <div class="meta-item">
        <dt>External ID</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.external_id) %></dd>
      </div>
      <div class="meta-item">
        <dt>Locked by</dt>
        <dd class="mono"><%= display(shopifyUpdateTask.locked_by) %></dd>
      </div>
      <div class="meta-item">
        <dt>Locked at</dt>
        <dd><%= display(shopifyUpdateTask.locked_at) %></dd>
      </div>
      <div class="meta-item">
        <dt>Processed</dt>
        <dd><%= display(shopifyUpdateTask.processed_at) %></dd>
      </div>
      <div class="meta-item">
        <dt>Completed</dt>
        <dd><%= display(shopifyUpdateTask.completed_at) %></dd>
      </div>
      <div class="meta-item">
        <dt>Skipped</dt>
        <dd><%= display(shopifyUpdateTask.skipped_at) %></dd>
      </div>
      <div class="meta-item">
        <dt>Failed / manual review</dt>
        <dd><%= display(shopifyUpdateTask.failed_at) %></dd>
      </div>
      <div class="meta-item">
        <dt>Created</dt>
        <dd><%= display(shopifyUpdateTask.created_at) %></dd>
      </div>
    </dl>
  </section>

  <details class="card disclosure">
    <summary>
      <span>
        <span class="section-label">Technical data</span>
        <strong>Redacted task payload</strong>
      </span>
      <span class="disclosure-hint">Show payload</span>
    </summary>
    <pre class="code-panel"><%= payloadJson || '{}' %></pre>
  </details>

  <details class="card disclosure">
    <summary>
      <span>
        <span class="section-label">Executor data</span>
        <strong>Redacted result payload</strong>
      </span>
      <span class="disclosure-hint">Show result</span>
    </summary>
    <pre class="code-panel"><%= resultJson || '{}' %></pre>
  </details>
</section>
