# Orchestrator MVP

Minimal Express and EJS skeleton for the Wandini Orchestrator.

## Commands

```bash
npm run dev
npm start
npm run worker
```

Run `npm run migrate` to create the MySQL database and tables from `src/db/schema.sql`.

## Routes

- `GET /health`
- `GET /admin`
